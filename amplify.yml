version: 1

frontend:
  phases:
    build:
      commands:
        - echo "No build required for static ShareBite website"

  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'

  cache:
    paths: []